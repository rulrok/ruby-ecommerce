<% if @admin_province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_province.errors.count, "error") %> prohibited this
        admin_province from being saved:</h2>

      <ul>
        <% @admin_province.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>
<div class="row">
  <div class="col-md-6">
    <%= form_for [:admin, @admin_province], html: {class: 'form'} do |f| %>

        <div class="form-group">
          <%= f.label :pst %><br>
          <%= f.number_field :pst, class: 'form-control', step: 0.05, min: 0, max: 1 %>
        </div>
        <div class="form-group">
          <%= f.label :gst %><br>
          <%= f.number_field :gst, class: 'form-control', step: 0.05, min: 0, max: 1 %>
        </div>
        <div class="form-group">
          <%= f.label :hst %><br>
          <%= f.number_field :hst, class: 'form-control', step: 0.05, min: 0, max: 1 %>
        </div>

        <br/>

        <%= f.submit class: 'btn btn-success' %>

    <% end %>
  </div>
</div>
